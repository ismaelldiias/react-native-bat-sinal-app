import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  shortLogo: {
    height: '80%',
    width: '20%',
    resizeMode: 'contain'
  },
  logo: {
    height: 180,
    resizeMode: 'contain'
  }
});